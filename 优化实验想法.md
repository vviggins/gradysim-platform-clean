# 优化实验想法（Lyapunov 在线跳过策略）

## 论文式描述
我们在最优路径（由 BQ-NCO 离线生成）上引入在线 Lyapunov 跳过策略，以应对地震场景中的通信不确定性。无人机按既定航点序列飞行，但每个节点的服务决策完全基于实时反馈，无需预知节点失败率。记队列势函数 Q_t 表征“尚未完成的数据需求”或“未覆盖节点价值”，Lyapunov 函数 L(Q_t)=1/2·Q_t^2。在节点 i 的第 k 次尝试时，评估漂移上界 ΔL ≈ V·Q_t − p̂_i，其中 p̂_i 为当前节点的经验成功率估计（由最近尝试成败更新），V 为权衡参数。当 ΔL 为正且 k 达到允许重试阈值 K 时，判定继续尝试的代价大于对全局队列的收益，触发跳过，直接飞向下一个航点。成功时减小 Q_t，失败但未跳过则累积 Q_t（反映延误/未收敛的数据需求）。该在线决策兼顾收敛速度与服务比例，可显著降低长尾延迟与任务总时长。

## 白话说明
- 路线是离线最优的，但通信质量现场试，不做先验假设（不提前知道）。
- 每个节点尝试通信，失败会多试几次；当失败达到上限且“继续耗时间的代价”过高，就跳过去下一个点。
- 成功时把“未完成任务”的压力值减掉，失败则压力值增加；压力高时更偏向跳过，避免卡死。
- 目标是减少在单个高失败节点的时间/能耗浪费，提升整体时间效率和覆盖率。

## 善后机制（防止高价值节点永久被弃）
- 跳过的节点进入回访列表；第一圈结束且时间/电量允许时，再按列表回访（可按节点价值或剩余距离排序）。
- 回访时降低重试阈值或采用快速探测（小次数短停留），避免再次长时间阻塞。
- 记录跳过原因与回访成功率，作为附录说明“先避卡点、后择机补扫”，确保高价值节点不被永久遗弃。
- 如已知高价值节点，可赋予更高的最大尝试数或更高优先级回访，对审稿人的“高价值遗漏”疑问有交代。
